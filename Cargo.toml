[package]
name = "llvm-airfryer"
version = "0.1.0"
edition = "2024"

[dependencies]
dialoguer = { version = "0.12.0", features = ["fuzzy-select"] }
shellexpand = "3"
toml = "0.8"
serde = { version = "1", features = ["derive"] }
