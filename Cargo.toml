[package]
name = "llvm-airfryer"
version = "0.2.8"
edition = "2024"

[dependencies]
dialoguer = { version = "0.12.0", features = ["fuzzy-select"] }
serde = { version = "1", features = ["derive"] }
shellexpand = "3"
toml = "0.8"
